import { styled } from '@mui/material';
import { Typography } from 'components/atoms';

const StyledTAppTitle = styled(Typography)({
  flexGrow: 1,
  textAlign: 'center',
});

export { StyledTAppTitle };
